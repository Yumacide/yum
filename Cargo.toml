[package]
name = "yum"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
logos = "0.12.1"
clap = { version = "3.2.8", default-features = false, features = [ "std", "derive" ] }